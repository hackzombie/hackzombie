<%= form_for(@vote) do |f| %>
  <% if @vote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote.errors.count, "error") %> prohibited this vote from being saved:</h2>

      <ul>
      <% @vote.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%=current_user.id%>
  <div class="field">
    <%= f.label :hack_id %><br />
    <%= f.text_field :hack_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
